name: GitHub Pages

on:
    push:
        branches:
        - master-test

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v1
        - run: ./deploy_github_action.sh
          env:
              GITHUB_TOKEN: ${{ secrets.github_token }}
